{% import "finalTheme::content.CategoryTree" as Tree %}
{% set lang = services.sessionStorage.getLang() %}
<nav class="hauptmenue">
    <ul>
        {% set categories = services.category.getNavigationTree("item", lang) %}
        {{ Tree.get_nav_bar(categories, "") }}
    </ul>
</nav>
